# 未解决或待办问题

## 1. 任务详情页保存功能问题

**问题描述:**
- 在任务详情页修改任务内容(如添加子任务、修改标题等)后,点击"保存"按钮
- 虽然 API 调用成功(返回 200),但返回日历页后看不到更新的内容
- 任务数据没有在界面上刷新

**已完成的工作:**
1. ✅ 实现了表单变化检测 (`hasFormChanged` 计算属性)
2. ✅ 保存按钮根据表单变化动态改变样式(灰色→黑色)
3. ✅ 修复了重复任务的ID问题(使用 `originalTaskId` 而不是实例ID)
4. ✅ 在 payload 中添加了子任务数据 (`subtasks` 字段)
5. ✅ 保存成功后调用 `fetchTasksByDate` 重新加载任务数据

**当前状态:**
- API 调用日志显示:
  - `originalTaskId: 69` (正确)
  - `[TaskEdit] 任务更新成功`
  - `PUT /api/v1/tasks/69` 成功
- 页面成功返回日历页
- 日历页调用了 `onShow` 和 `selectDate` 重新筛选任务
- **但是**: 界面上任务内容没有更新

**可能的原因:**
1. 后端虽然返回成功,但可能没有真正保存数据
2. 前端缓存问题,`fetchTasksByDate` 可能使用了缓存数据
3. 重复任务的更新逻辑特殊,可能需要特定的 API 参数
4. 数据库事务问题或重复任务展开逻辑问题

**下一步调试建议:**
1. 检查后端日志,确认 `PUT /api/v1/tasks/69` 是否真的写入了数据库
2. 直接查询数据库,看任务 ID=69 的数据是否被更新
3. 检查 `taskStore.fetchTasksByDate` 的实现,是否有缓存机制
4. 尝试在保存后清除相关缓存
5. 检查后端重复任务的更新逻辑是否正确处理了子任务字段

**临时解决方案:**
- 暂时注释掉了重复任务保存确认弹窗的逻辑
- 所有保存操作直接调用 `save()` 函数

**相关文件:**
- `D:\MyProject\Planning-app\frontend\Planning-app\pages\calendar\task-edit.vue`
  - `save()` 函数 (line 1670+)
  - `handleSave()` 函数 (line 1820+)
  - `originalTaskId` 定义 (line 499)

**创建时间:** 2026-03-02

---

## 2. 重复任务保存确认弹窗设计问题

**问题描述:**
- 原计划在修改重复任务时显示确认对话框,让用户选择更新范围
- 弹窗显示时格式不正确,文字显示有问题
- 触发条件也需要优化

**当前状态:**
- 已临时注释掉弹窗逻辑,等保存功能正常后再实现

**待实现功能:**
1. 设计合理的弹窗UI
2. 实现两种更新选项:
   - 完整更改此条重复计划
   - 更改当天及未来计划
3. 根据用户选择调用不同的 API

**相关文件:**
- `D:\MyProject\Planning-app\frontend\Planning-app\pages\calendar\task-edit.vue` (line 431-475, 1819-1855)

**创建时间:** 2026-03-02

---

## 3. 工作日角标数据更新策略（未来年份）

**问题描述:**
- 当前工作日角标（"休"/"班"）的数据仅覆盖2026年（39条记录：33个法定节假日 + 6个调休补班日）
- 数据来源于国务院办公厅发布的《关于2026年部分节假日安排的通知》
- 需要规划如何更新2027年、2028年及后续年份的工作日数据

**当前实现:**
- 数据表：`work_days` (字段：date, type, year, holiday_name, remark)
- Seeder 文件：`backend/src/seeders/20260306152306-work-days-2026.js`
- API 接口：
  - `GET /api/v1/holidays/workdays/year/:year` - 获取指定年份工作日数据
  - `GET /api/v1/holidays/workdays/range?start=YYYY-MM-DD&end=YYYY-MM-DD` - 获取日期范围工作日数据

**可选方案:**

### 方案1：手动维护 Seeder 文件（推荐短期使用）
- **优点**: 可靠、可控、数据准确
- **缺点**: 需要每年手动更新
- **实施步骤**:
  1. 每年国务院发布节假日通知后（通常在12月）
  2. 参照现有 `20260306152306-work-days-2026.js` 创建新年份 seeder
  3. 运行 `npx sequelize-cli db:seed --seed <新seeder文件名>`
- **适用场景**: 短期内（2-3年）的数据维护

### 方案2：实现爬虫自动抓取
- **优点**: 自动化程度高
- **缺点**: 依赖外部网站结构，不稳定
- **数据源**:
  - 国务院官网：http://www.gov.cn/zhengce/content/
  - 人民网官方通知
  - 新华网官方通知
- **技术实现**:
  - 使用 `cheerio` 或 `puppeteer` 爬取HTML
  - 解析节假日调整通知文本
  - 自动生成 Seeder 数据
- **风险**: 官网结构变化导致爬虫失效

### 方案3：管理后台手动录入
- **优点**: 灵活、可控、支持人工审核
- **缺点**: 需要开发管理后台
- **技术实现**:
  1. 创建管理员权限系统
  2. 开发工作日管理界面（增删改查）
  3. 实现数据校验（防止日期重复、格式错误）
  4. 支持批量导入Excel文件
- **适用场景**: 长期运营，需要多人协作管理

### 方案4：接入第三方API
- **优点**: 无需维护数据，自动更新
- **缺点**: 依赖第三方服务稳定性，可能收费
- **可选API**:
  - 阿里云日历服务（需收费）
  - 免费节假日API（稳定性待验证）
- **技术实现**:
  - 定期调用第三方API拉取数据
  - 缓存到本地数据库（减少API调用次数）
  - 设置过期时间，每年12月自动更新次年数据

**推荐方案:**
- **2026-2028年**: 使用方案1（手动 Seeder）
- **2029年起**: 根据用户规模决定：
  - 用户 < 1000：继续使用方案1
  - 用户 1000-10000：实现方案3（管理后台）
  - 用户 > 10000：考虑方案4（第三方API）

**技术考虑:**
- 数据库设计已支持多年份数据（year 字段，索引已建立）
- API 已支持按年份和日期范围查询
- 前端已实现工作日角标显示逻辑（`useCalendar.js` + `CalendarBar.vue`）
- 只需要维护数据源，无需修改代码

**相关文件:**
- 数据库迁移：`backend/src/migrations/20260306151755-create-work-days.js`
- 数据模型：`backend/src/models/workDay.js`
- Seeder 模板：`backend/src/seeders/20260306152306-work-days-2026.js` （可作为新年份模板）
- 业务逻辑：`backend/src/services/holidayService.js` (line 144-176)
- 前端调用：`frontend/Planning-app/composables/useCalendar.js` (line 264-270)
- UI显示：`frontend/Planning-app/components/calendar/CalendarBar.vue` (line 121-124)

**创建时间:** 2026-03-07

---

## 4. TaskRepository 重复更新问题（性能问题）

**问题描述:**
- 用户从"任务详情"页保存任务并返回日历页后，同一个任务会被 **重复更新3次**
- 每次更新都触发后端同步，导致不必要的API请求
- 虽然功能正常（任务保存成功），但存在性能浪费和潜在的数据覆盖风险

**现象（从测试日志）:**
```
160→ TaskRepository.js:158 [TaskRepository] 更新任务: 测试  ← 第1次异常更新
165→ TaskRepository.js:158 [TaskRepository] 更新任务: 测试  ← 第2次异常更新
170→ TaskRepository.js:158 [TaskRepository] 更新任务: 测试  ← 第3次异常更新
```

**根本原因分析:**
1. **用户点击保存按钮只会触发1次更新**（已确认，正常）
2. **异常的3次更新发生在用户返回日历页之后**
3. **可能的触发源**：
   - `index.vue` 的 watch 监听 `selectedDate`（line 488-496）
   - watch 触发 `fetchTasksByDate` → 更新 `tasks.value`
   - 某个 computed 或其他 watch 监听 `tasks` 并触发更新
   - 形成响应式循环，重复3次后停止

**影响:**
- 🔴 **性能问题**：每次保存任务后产生额外的2次后端请求（总共3次）
- 🔴 **数据一致性风险**：如果用户快速操作，可能导致数据覆盖
- 🔴 **后端压力**：不必要的重复请求增加服务器负担

**已排查的位置:**
1. ✅ `task-edit.vue` 无 watch 监听（已确认）
2. ✅ `TaskRepository.update()` 本身无问题（单次调用正常）
3. ✅ `taskStore.fetchTasksByDate()` 只是读取数据（无更新操作）
4. ⏸️ `index.vue` 的 watch 和 computed（待深入排查）
5. ⏸️ `TaskQuadrantView.vue` 组件（待排查）
6. ⏸️ `useDragDrop.js` 和 `useCalendar.js` composable（待排查）

**解决方案:**

### 方案1：添加调用栈日志（诊断用）
**目的**: 找出重复调用的源头

**修改文件**: `frontend/Planning-app/repositories/TaskRepository.js`（Line 152）

**修改内容**:
```javascript
async update(id, data) {
  // 添加调用栈日志，找出调用源
  const stack = new Error().stack;
  console.log('[TaskRepository] update 调用');
  console.log('[TaskRepository] 调用栈:', stack);

  // ... 原有代码
}
```

### 方案2：防抖去重（快速修复）
**目的**: 防止短时间内对同一任务的重复更新

**修改文件**: `frontend/Planning-app/repositories/TaskRepository.js`

**修改内容**:
1. 在 constructor 中添加：
   ```javascript
   this.lastUpdateTime = new Map()  // taskId → timestamp
   ```

2. 在 update() 方法开头添加防重复逻辑：
   ```javascript
   async update(id, data) {
     // 防重复更新：同一任务500ms内只更新一次
     const now = Date.now()
     const lastUpdate = this.lastUpdateTime.get(id)
     if (lastUpdate && (now - lastUpdate) < 500) {
       console.log(`[TaskRepository] 跳过重复更新（距上次 ${now - lastUpdate}ms）:`, id)
       return this.memoryCache.get(id)
     }
     this.lastUpdateTime.set(id, now)

     // ... 原有代码
   }
   ```

**预期效果**:
- ✅ 同一任务在500ms内只更新一次
- ✅ 从4次请求减少到1次请求
- ✅ 不影响用户正常操作（500ms间隔足够长）

### 方案3：根治响应式循环（彻底解决）
**目的**: 找到并修复导致循环的根本原因

**待排查的文件**:
1. `frontend/Planning-app/pages/calendar/index.vue`
   - 检查所有 computed 是否在计算过程中调用了 updateTask
   - 检查 watch 的触发链条

2. `frontend/Planning-app/components/calendar/TaskQuadrantView.vue`
   - 检查是否有 watch 监听 tasks prop 并触发更新

3. `frontend/Planning-app/composables/useDragDrop.js`
   - 检查拖拽结束后的更新逻辑

4. `frontend/Planning-app/composables/useCalendar.js`
   - 检查 watch selectedDate 的副作用

**需要时间**: 1-2小时深入排查

**推荐执行顺序:**
1. **立即执行**: 方案1（5分钟）→ 重新测试获取调用栈
2. **快速修复**: 方案2（10分钟）→ 验证重复更新消失
3. **后续优化**: 方案3（1-2小时）→ 根治问题

**相关文件:**
- `frontend/Planning-app/repositories/TaskRepository.js` (Line 152-180)
- `frontend/Planning-app/pages/calendar/index.vue` (Line 488-496)
- `frontend/Planning-app/store/task.js` (Line 114-133)
- `frontend/Planning-app/composables/useTaskQuadrant.js` (Line 226-250)

**测试日志文件:**
- `D:\MyProject\Planning设计\测试日志\测试前端日志.txt` (Line 160-174)

**创建时间:** 2026-03-09

**优先级:** P1（性能问题，不影响功能，但需要优化）

---

## 5. 任务详情页修改重复规则后保存按钮不响应 ⭐ 高优先级

**问题描述:**
- 在任务详情页点击"重复"选项，修改重复规则（如从"每日"改为"每周"或"每月"）
- 页面显示已更新（右侧显示"每周"或"每月"）
- **但是"保存"按钮保持灰色禁用状态，无法点击保存**
- 修改其他字段（如标题、描述）时，保存按钮正常变为高亮

**问题影响:**
- 🔴 **功能阻塞**: 用户无法保存重复规则的修改，严重影响用户体验
- 🔴 **优先级**: P0（最高优先级，核心功能失效）

**已完成的诊断和修复尝试:**

### 修复轮次1：补充工具函数导入 (commit dcf9845)
- **问题**: `formatDate is not defined`, `syncRrule is not defined`
- **原因**: Phase 5重构时遗漏了函数体内的调用替换
- **解决**: 添加import + 批量替换15处syncRrule调用
- **结果**: ❌ 保存按钮问题依然存在

### 修复轮次2：新增重复规则变化检测 (commit 582a6ad)
- **问题**: hasFormChanged未检测repeatRuleManager中的状态变化
- **解决**: 创建hasRepeatChanged计算属性，集成到hasFormChanged
- **结果**: ❌ 保存按钮问题依然存在

### 修复轮次3：修复用户修改被覆盖 (commit dc0ac29)
- **问题**: confirmRepeatSettings调用loadFromRrule覆盖用户修改
- **根本原因**: 
  ```javascript
  function confirmRepeatSettings() {
    repeatRuleManager.loadFromRrule(form.value.rrule || '');  // ❌ 覆盖用户修改
    showRepeatSheet.value = false;
  }
  ```
- **解决**: 移除错误的loadFromRrule调用，保留用户在repeatRuleManager中的修改
- **结果**: ❌ 保存按钮问题依然存在

### 修复轮次4：修复Vue响应式依赖追踪 (commit c0ce764)
- **问题**: hasRepeatChanged计算属性从未被触发（测试日志无输出）
- **根本原因**: Vue无法追踪generateRrule()函数调用内部的响应式变量
- **解决**: 在computed内直接访问所有响应式变量
  ```javascript
  const hasRepeatChanged = computed(() => {
    // ✅ 直接访问响应式变量，让Vue建立依赖追踪
    const repeatMode = repeatRuleManager.repeatMode.value
    const repeatInterval = repeatRuleManager.repeatInterval.value
    const repeatWeekDays = repeatRuleManager.repeatWeekDays.value
    // ... 其他变量
    
    const currentRrule = repeatRuleManager.generateRrule() || ''
    const originalRrule = originalForm.value.rrule || ''
    return currentRrule !== originalRrule
  })
  ```
- **结果**: ❌ 保存按钮问题依然存在

### 修复轮次5：修复hasFormChanged被本地定义覆盖 (commit 9bca1fb) ⭐ 最新尝试
- **问题**: task-edit.vue中有两个hasFormChanged定义
  1. useTaskForm.js导出的hasFormChanged（✅ 包含重复规则检测）
  2. task-edit.vue本地定义的hasFormChanged（❌ 未检测重复规则）
  - 模板使用的是本地定义的版本
- **解决**: 
  1. 在taskFormApi解构中添加hasFormChanged（从useTaskForm导入）
  2. 删除task-edit.vue本地的hasFormChanged定义
  3. 删除重复的originalForm和originalSubtasks定义
  4. 删除onMounted中重复的赋值逻辑
- **结果**: ❌ 保存按钮问题依然存在

**当前状态:**
- 已提交5轮修复（共5个commit）
- 所有已知的逻辑问题都已修复
- 用户确认问题仍然存在
- 需要重新审视整个数据流和响应式机制

**测试现象（用户反馈）:**
1. 打开任务详情页
2. 点击"重复"选项
3. 修改重复模式（如从"每日"改为"每月"）
4. 页面右侧显示更新为"每月"（✅ 正常）
5. 控制台输出 `[confirmRepeatSettings]` 日志（✅ 正常）
6. **但是"保存"按钮保持灰色禁用状态**（❌ 异常）
7. **控制台没有输出 `[hasRepeatChanged]` 或 `[hasFormChanged]` 日志**（❌ 关键异常）

**核心问题:**
`hasFormChanged` 计算属性没有被触发重新计算，即使其依赖的响应式变量已经改变。

**待深入排查的方向:**

### 方向1：检查taskFormApi的返回结构
可能问题：useTaskForm.js返回的hasFormChanged不是响应式的computed ref

**检查步骤**:
1. 读取 `composables/useTaskForm.js` 的return部分
2. 确认hasFormChanged是否正确导出
3. 确认导出的是computed对象，不是普通函数

### 方向2：检查repeatRuleManager的初始化时序
可能问题：repeatRuleManager在hasFormChanged初始化之前就已经创建，导致依赖追踪失效

**检查步骤**:
1. 查看useTaskForm.js中repeatRuleManager和hasFormChanged的定义顺序
2. 确认hasFormChanged能访问到repeatRuleManager的最新实例

### 方向3：检查form.value.rrule的赋值时机
可能问题：confirmRepeatSettings关闭弹窗后，form.value.rrule没有立即更新

**检查步骤**:
1. 在confirmRepeatSettings中添加日志，检查关闭弹窗后form.value.rrule的值
2. 检查是否需要手动触发form.value.rrule的更新

### 方向4：添加详细的调用链日志
目的：找出为什么hasFormChanged没有被触发

**实施步骤**:
1. 在hasFormChanged计算属性的开头添加日志
2. 在repeatMode.value赋值时添加日志
3. 在confirmRepeatSettings的每一步添加日志
4. 重新测试，分析完整的调用链

### 方向5：强制触发hasFormChanged（workaround）
临时解决方案：在confirmRepeatSettings中手动触发hasFormChanged

```javascript
function confirmRepeatSettings() {
  console.log('[confirmRepeatSettings] 确认重复设置')
  
  // 手动更新form.value.rrule，触发hasFormChanged
  form.value.rrule = repeatRuleManager.generateRrule() || ''
  
  showRepeatSheet.value = false
}
```

**相关文件:**
- `frontend/Planning-app/composables/useTaskForm.js` (hasFormChanged定义: Line 154-179)
- `frontend/Planning-app/composables/useRepeatRuleManager.js` (generateRrule: Line 121-152)
- `frontend/Planning-app/pages/calendar/task-edit.vue` (confirmRepeatSettings: Line 1365-1375)
- `frontend/Planning-app/pages/calendar/task-edit.vue` (保存按钮: Line 196-197)

**相关commit:**
- dcf9845: fix(task-edit): 补充工具函数导入和syncRrule调用修复
- 582a6ad: fix(useTaskForm): 修复重复规则修改后hasFormChanged未检测到变化
- dc0ac29: fix(task-edit): 修复重复规则修改被覆盖的BUG + 添加详细调试日志
- c0ce764: fix(useTaskForm): 修复hasRepeatChanged响应式依赖追踪问题
- 9bca1fb: fix(task-edit): 修复hasFormChanged被本地定义覆盖导致重复规则变化未检测的问题

**测试日志:**
- 位置: `D:\MyProject\Planning设计\测试日志\测试前端日志.txt`
- 关键日志: 
  ```
  [confirmRepeatSettings] 确认重复设置，当前状态: {repeatMode: 'monthly', repeatInterval: 1, form.value.rrule: 'FREQ=DAILY'}
  [confirmRepeatSettings] 关闭弹窗，不执行 loadFromRrule（用户修改已在 repeatRuleManager 中）
  ```
  - ⚠️ 缺失: `[hasRepeatChanged]` 和 `[hasFormChanged]` 日志

**建议后续解决时间:**
- 预计排查时间: 2-3小时
- 优先级: P0（最高优先级）
- 建议在下次会话优先处理

**创建时间:** 2026-03-10
**最后更新:** 2026-03-10

