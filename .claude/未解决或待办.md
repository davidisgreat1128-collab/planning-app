# 未解决或待办事项

> **最后更新**: 2026-02-26
> **更新者**: Claude Sonnet 4.5

---

## 🔥 特别重要的事

### 1. 服务器和数据库统一调整
**优先级**: ⭐⭐⭐⭐⭐ P0

**描述**: 等前端做的差不多了，要统一调整服务器和数据库（后端代码）

**详细说明**:
- 目前后端是本地开发环境（localhost:3000）
- 需要部署到正式服务器
- 数据库需要从开发环境迁移到生产环境
- 涉及文件：
  - `backend/.env.development` → `.env.production`
  - `backend/src/config/database.js` - 数据库连接配置
  - 所有 API 接口的 baseURL 配置

**TODO**:
- [ ] 确定生产服务器地址和端口
- [ ] 配置生产数据库（MySQL）
- [ ] 运行生产环境数据库迁移
- [ ] 更新前端 API baseURL 配置
- [ ] 配置 CORS、安全策略
- [ ] 配置 PM2 或其他进程管理工具

**相关文档**:
- `backend/README.md` - 部署说明
- `.claude/CURRENT_STATUS.md` - 当前环境配置

---

## 🐛 BUG 问题

### 1. 删除规划时无法删除手动创建的任务 ❌ **已修复**
**优先级**: ⭐⭐⭐ P1
**状态**: ✅ 已修复（2026-02-26）

**描述**: 删除一个规划，并勾选"删除该规划下所有的任务"，只能删除该规划系统批量创建的任务，不能删除该规划下手动单独创建的任务。

**根本原因**:
- `planStore.deletePlan()` 方法有 TODO 注释，删除任务的逻辑未实现
- `category-drawer.vue` 的 `onDeletePlanConfirm()` 也有 TODO，未调用任务删除

**修复方案**:
- ✅ 实现了 `planStore.deletePlan(planId, deleteWithTasks)` 完整逻辑
- ✅ 支持删除 localStorage 任务和后端任务
- ✅ 支持将任务改为无分类（deleteWithTasks=false）
- ✅ 修改了 `category-drawer.vue` 调用删除逻辑

**修复 Commit**: `bbdebd2` - "fix(plan): 实现删除规划时清理关联任务的逻辑"

**涉及文件**:
- `frontend/Planning-app/store/plan.js`
- `frontend/Planning-app/components/category-drawer.vue`

---

### 2. 任务详情页面重复规则显示错误
**优先级**: ⭐⭐⭐ P1
**状态**: ⚠️ 待修复

**描述**: 单条任务的详情页面，明明开了重复，但是该任务的详情页面显示的是"未开启"

**重现步骤**:
1. 创建一个新任务，设置为"每日重复"
2. 保存任务
3. 再次打开该任务的详情页面
4. 显示重复规则为"未开启"

**可能原因**:
- 任务编辑页面 `task-edit.vue` 在加载任务数据时，未正确解析 `rrule` 字段
- 可能是 `repeatMode` 的初始化逻辑有问题
- 需要检查 `form` 对象的 `rrule` 字段是否正确传递

**需要检查的文件**:
- `frontend/Planning-app/pages/calendar/task-edit.vue` - 任务编辑页
- `frontend/Planning-app/components/task/AddTaskPanel.vue` - 快速添加面板（检查重复规则的处理逻辑）

**TODO**:
- [ ] 检查任务加载时是否正确解析 `rrule` 字段
- [ ] 检查 `repeatMode` 的映射逻辑（从 rrule 字符串转换为前端的 mode）
- [ ] 确认后端返回的 `rrule` 格式是否符合预期
- [ ] 添加 console.log 调试任务数据

**相关代码位置**:
- `task-edit.vue` - 需要检查 onMounted 或 watch 中加载任务的逻辑
- `AddTaskPanel.vue:1286` - `buildRrule(rd)` 函数生成 rrule 的逻辑

---

## 📋 还未解决的事

### 1. 前端功能待完善

#### 1.1 AddTaskPanel 时间段/重复/提醒功能
**优先级**: ⭐⭐⭐⭐ P0
**状态**: ⚠️ 进行中（placeholder）

**描述**: `AddTaskPanel.vue` 快速添加面板的3个功能按钮是 placeholder

**TODO**:
- [ ] `onTimeTap()` - 实现时间段选择弹窗（isAllDay=false，选择开始/结束时间）
- [ ] `onRepeatTap()` - 实现重复规则底部弹窗（参考 task-edit.vue 的逻辑）
- [ ] `onReminderTap()` - 实现提醒时间选择弹窗

**涉及文件**:
- `frontend/Planning-app/components/task/AddTaskPanel.vue`

**参考**:
- `frontend/Planning-app/pages/calendar/task-edit.vue` - 已有的重复规则选择逻辑

---

#### 1.2 TabBar 图标配置
**优先级**: ⭐⭐ P2
**状态**: ⚠️ 待处理

**描述**: `pages.json` 中 TabBar 的 `iconPath` 和 `selectedIconPath` 没有配置真实图标文件

**TODO**:
- [ ] 设计或选择合适的图标（做计划/视图/专注/我的）
- [ ] 将图标放入 `static/` 目录
- [ ] 更新 `pages.json` 中的 iconPath 配置

**涉及文件**:
- `frontend/Planning-app/pages.json`

---

#### 1.3 闹铃功能
**优先级**: ⭐⭐⭐ P1
**状态**: ⚠️ 待开发

**描述**: 闹铃功能（Phase 4）前端页面尚未开发

**TODO**:
- [ ] 创建闹铃设置页面
- [ ] 集成后端 `/api/v1/alarms` 接口
- [ ] 实现闹铃音频播放功能
- [ ] 实现闹铃提醒通知

**后端接口**:
- ✅ `GET /api/v1/alarms` - 获取闹铃列表
- ✅ `POST /api/v1/alarms` - 创建闹铃
- ✅ `DELETE /api/v1/alarms/:id` - 删除闹铃

---

### 2. 后端功能待完善

#### 2.1 日志列表 API 参数校验
**优先级**: ⭐ P3
**状态**: ⚠️ 已知问题

**描述**: `routes/log.js` GET 接口要求 `date` 或 `start` 参数必填，前端不能裸调 `getLogs({})`

**影响**: 前端必须传递日期参数才能获取日志列表

**TODO**:
- [ ] 确认这是否是预期行为
- [ ] 如果需要支持无参数查询所有日志，需要修改后端校验逻辑

**涉及文件**:
- `backend/src/routes/log.js` - `getLogsQuerySchema`

---

### 3. 已知的技术债务

#### 3.1 模板生成任务存储方式
**优先级**: ⭐⭐ P2
**状态**: ⚠️ 技术债务

**描述**: 从模板生成的任务目前存储在 localStorage，ID 格式为 `task_timestamp_...`

**问题**:
- localStorage 和后端 API 双存储系统，逻辑复杂
- 不同设备间无法同步
- 数据容易丢失

**建议**:
- 考虑将模板生成的任务也存储到后端
- 或者实现 localStorage 到后端的自动同步机制

**涉及文件**:
- `frontend/Planning-app/store/task.js`
- `frontend/Planning-app/store/plan.js`

---

## 📊 已修复的问题（归档）

### ✅ 重复任务不显示"重复事件"标签
**修复日期**: 2026-02-26
**Commit**: `1df8536`

**问题**: 创建每日重复任务后，任务列表不显示"重复事件"橙色标签

**根本原因**: 后端返回 TaskOccurrence 时缺少 `isRecurring` 字段

**解决方案**: 在 `taskService.js` 中合并父任务的关键字段到实例数据

---

### ✅ 新创建任务左边没有颜色条
**修复日期**: 2026-02-26
**Commit**: `237159f`

**问题**: 新创建的任务左边没有四象限优先级颜色条

**根本原因**: CSS 只定义了 priority-high 和 priority-medium，缺少 priority-low 和默认颜色

**解决方案**: 添加完整的四象限颜色样式（红/蓝/黄/绿）

---

## 📝 文档更新规范

每次修复 BUG 或完成待办事项后：

1. 将已完成的项目移到"📊 已修复的问题（归档）"部分
2. 更新文档顶部的"最后更新"日期
3. 记录相关的 Commit ID
4. 如果是新发现的问题，添加到对应的 BUG 或待办部分

---

**相关文档**:
- `.claude/CURRENT_STATUS.md` - 项目当前状态
- `.claude/CLAUDE.md` - Claude 协作指南
- `CHANGELOG.md` - 版本变更日志
